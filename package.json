{
  "name": "compound-subgraph",
  "version": "1.0.0",
  "description": "Compound Subgraph",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/GraphiteTools/compound-subgraph.git"
  },
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ graphitetools/compound"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.0"
  }
}
